---
description: Shadow scaling with φ and z-index architecture
paths:
  - packages/styles/src/**/*.css
---

# Elevation

## Shadow

Scale with φ as stepped progression — see actual values in `packages/styles/src/tokens/layout.css`

## Z-Index

Use semantic token names — never use arbitrary z-index (z-[99], z-[9999])
See actual values in `packages/styles/src/tokens/layout.css`
