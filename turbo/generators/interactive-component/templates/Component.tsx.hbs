"use client";

import type { ComponentPropsWithRef, ReactNode } from "react";

import { {{pascalCase racName}} as RAC{{pascalCase name}}, composeRenderProps } from "react-aria-components";
import type { {{pascalCase racName}}RenderProps } from "react-aria-components";

import { clsx } from "clsx";

import { bem } from "../../utils/bem";

import type { {{pascalCase name}}VariantsProps } from "./{{dashCase name}}.variants";

export type {{pascalCase name}}Props = {{pascalCase name}}VariantsProps & {
  children?: ReactNode | ((renderProps: {{pascalCase racName}}RenderProps) => ReactNode);
  className?: string | ((renderProps: {{pascalCase racName}}RenderProps) => string);
} & Omit<ComponentPropsWithRef<typeof RAC{{pascalCase name}}>, "className" | "children">;

const {{pascalCase name}} = (props: Readonly<{{pascalCase name}}Props>) => {
  const { children, className, ref, size, variant, ...rest } = props;

  const baseClassName = bem({
    block: "fri-{{dashCase name}}",
    modifiers: {
      variant,
      size,
      disabled: rest.isDisabled,
    },
  });

  const {{camelCase name}}ClassName = composeRenderProps(className, (consumerClassName) => clsx(baseClassName, consumerClassName));

  return (
    <RAC{{pascalCase name}} {...rest} data-slot="{{dashCase name}}" className={ {{camelCase name}}ClassName} ref={ref}>
      {children}
    </RAC{{pascalCase name}}>
  );
};

{{pascalCase name}}.displayName = "{{pascalCase name}}";

export { {{pascalCase name}} };
