@layer components {
  .{{dashCase name}} {
    @apply inline-flex items-center justify-center;
    @apply leading-none font-medium whitespace-nowrap;
    @apply cursor-pointer border border-transparent select-none no-highlight;
    @apply transition duration-(--duration-hover) ease-out motion-reduce:transition-none;

    --{{dashCase name}}-bg: var(--color-primary);
    --{{dashCase name}}-bg-hover: var(--color-primary-hover);
    --{{dashCase name}}-bg-pressed: var(--color-primary-active);
    --{{dashCase name}}-fg: var(--color-primary-foreground);

    @apply bg-(--{{dashCase name}}-bg) text-(--{{dashCase name}}-fg);
    @apply gap-[calc(1rem/2.058)] px-[calc(1rem)] py-[calc(1rem/2.058)] text-base;
    @apply rounded-[calc(1em*1.272*2/4)];

    &:focus-visible,
    &[data-focus-visible] {
      @apply focus-ring;
    }

    &:disabled,
    &[data-disabled] {
      @apply status-disabled;
    }

    @media (hover: hover) {
      &:hover:not(:disabled):not(:active),
      &[data-hovered] {
        @apply bg-(--{{dashCase name}}-bg-hover);
      }
    }

    &:active:not(:disabled),
    &[data-pressed] {
      @apply bg-(--{{dashCase name}}-bg-pressed) duration-(--duration-press);
    }
  }

  .{{dashCase name}}--primary {
    --{{dashCase name}}-bg: var(--color-primary);
    --{{dashCase name}}-bg-hover: var(--color-primary-hover);
    --{{dashCase name}}-bg-pressed: var(--color-primary-active);
    --{{dashCase name}}-fg: var(--color-primary-foreground);
  }

  .{{dashCase name}}--secondary {
    --{{dashCase name}}-bg: var(--color-secondary);
    --{{dashCase name}}-bg-hover: var(--color-secondary-hover);
    --{{dashCase name}}-bg-pressed: var(--color-secondary-active);
    --{{dashCase name}}-fg: var(--color-secondary-foreground);
  }

  .{{dashCase name}}--ghost {
    --{{dashCase name}}-bg: transparent;
    --{{dashCase name}}-bg-hover: var(--color-muted);
    --{{dashCase name}}-bg-pressed: var(--color-muted-hover);
    --{{dashCase name}}-fg: var(--color-foreground);
  }

  .{{dashCase name}}--outline {
    --{{dashCase name}}-bg: transparent;
    --{{dashCase name}}-bg-hover: var(--color-muted);
    --{{dashCase name}}-bg-pressed: var(--color-muted-hover);
    --{{dashCase name}}-fg: var(--color-foreground);

    @apply border-border;
  }

  .{{dashCase name}}--success {
    --{{dashCase name}}-bg: var(--color-success);
    --{{dashCase name}}-bg-hover: var(--color-success-hover);
    --{{dashCase name}}-bg-pressed: var(--color-success-active);
    --{{dashCase name}}-fg: var(--color-success-foreground);
  }

  .{{dashCase name}}--warning {
    --{{dashCase name}}-bg: var(--color-warning);
    --{{dashCase name}}-bg-hover: var(--color-warning-hover);
    --{{dashCase name}}-bg-pressed: var(--color-warning-active);
    --{{dashCase name}}-fg: var(--color-warning-foreground);
  }

  .{{dashCase name}}--danger {
    --{{dashCase name}}-bg: var(--color-danger);
    --{{dashCase name}}-bg-hover: var(--color-danger-hover);
    --{{dashCase name}}-bg-pressed: var(--color-danger-active);
    --{{dashCase name}}-fg: var(--color-danger-foreground);
  }

  .{{dashCase name}}--info {
    --{{dashCase name}}-bg: var(--color-info);
    --{{dashCase name}}-bg-hover: var(--color-info-hover);
    --{{dashCase name}}-bg-pressed: var(--color-info-active);
    --{{dashCase name}}-fg: var(--color-info-foreground);
  }

  .{{dashCase name}} [slot="icon"],
  .{{dashCase name}} [slot="icon-start"],
  .{{dashCase name}} [slot="icon-end"] {
    @apply size-match-font;
  }

  .{{dashCase name}}--size-sm {
    @apply gap-[calc(0.875rem/2.058)] px-[calc(0.875rem)] py-[calc(0.875rem/2.058)] text-sm;
  }

  .{{dashCase name}}--size-md {
    @apply gap-[calc(1rem/2.058)] px-[calc(1rem)] py-[calc(1rem/2.058)] text-base;
  }

  .{{dashCase name}}--size-lg {
    @apply gap-[calc(1.25rem/2.058)] px-[calc(1.25rem)] py-[calc(1.25rem/2.058)] text-xl;
  }

  .{{dashCase name}}--radius-none {
    @apply rounded-none;
  }

  .{{dashCase name}}--radius-sm {
    @apply rounded-[calc(1em*1.272/4)];
  }

  .{{dashCase name}}--radius-md {
    @apply rounded-[calc(1em*1.272*2/4)];
  }

  .{{dashCase name}}--radius-lg {
    @apply rounded-[calc(1em*1.272*3/4)];
  }

  .{{dashCase name}}--radius-full {
    @apply rounded-full;
  }
}
