import type { Meta, StoryObj } from "@storybook/react";

import { {{pascalCase name}} } from "./{{pascalCase name}}";

/*
 * ⚠️ SCAFFOLD TEMPLATE — REQUIRES CUSTOMIZATION
 *
 * This file ships with Button-style defaults (children="text", onPress, 8 variants).
 * YOU MUST rewrite `args`, `argTypes`, and stories to match YOUR component's actual API:
 *
 * 1. Remove `children`/`onPress` if the component doesn't use them (e.g. Input uses `placeholder`)
 * 2. Rewrite `variant` options + 4-part description to match this component's real variants
 * 3. Update variant TypeScript summary string to match `{{pascalCase name}}VariantsProps`
 * 4. Remove or add argTypes (isInvalid, isReadOnly, isRequired, isFullWidth, etc.)
 * 5. Rewrite render blocks with realistic props (NOT template defaults)
 *
 * Reference: Input/Button/Badge stories as live examples. Never ship this scaffold verbatim.
 */

const meta = {
  title: "Components/{{pascalCase name}}",
  component: {{pascalCase name}},
  tags: ["autodocs"],
  parameters: {
    layout: "centered",
  },
  args: {
    children: "{{pascalCase name}}",
    variant: "primary",
    size: "md",
  },
  argTypes: {
    children: {
      control: "text",
      description: "{{pascalCase name}} content",
      table: {
        type: { summary: "ReactNode" },
        category: "Children",
      },
    },
    variant: {
      control: "select",
      options: ["primary", "secondary", "ghost", "outline", "success", "warning", "danger", "info"],
      // TODO: rewrite this description following the 4-part pattern in .claude/rules/storybook.md (intent + groups + modifiers + usage).
      // Length: 50-120 words. Bold headings. End with "Use X for Y" guidance. See Button/Badge for reference.
      description: "Visual style. **Base:** TODO. **Status:** TODO. Use primary for TODO, danger for TODO.",
      table: {
        type: { summary: '"primary" | "secondary" | "ghost" | "outline" | "success" | "warning" | "danger" | "info"' },
        defaultValue: { summary: "primary" },
        category: "Style Variants",
      },
    },
    size: {
      control: "select",
      options: ["sm", "md", "lg"],
      description: "The size of the {{dashCase name}}",
      table: {
        type: { summary: '"sm" | "md" | "lg"' },
        defaultValue: { summary: "md" },
        category: "Style Variants",
      },
    },
    isDisabled: {
      control: "boolean",
      description: "Whether the {{dashCase name}} is disabled",
      table: {
        type: { summary: "boolean" },
        defaultValue: { summary: "false" },
        category: "State",
      },
    },
    onPress: {
      control: false,
      description: "Handler called when pressed",
      table: {
        type: { summary: "(e: PressEvent) => void" },
        category: "Events",
      },
    },
    className: {
      control: "text",
      description: "Additional CSS classes",
      table: {
        type: { summary: "string" },
        category: "Styling",
      },
    },
  },
} satisfies Meta<typeof {{pascalCase name}}>;

type Story = StoryObj<typeof meta>;

const Default: Story = {
  parameters: {
    docs: {
      source: {
        code: `import { {{pascalCase name}} } from "@fried-ui/react";

const Basic = () => {
  return <{{pascalCase name}} onPress={() => console.log("pressed")}>Click me</{{pascalCase name}}>;
};`,
      },
    },
  },
};

const Variants: Story = {
  parameters: {
    docs: {
      source: {
        code: `import { {{pascalCase name}} } from "@fried-ui/react";

const Variants = () => {
  return (
    <div className="flex flex-wrap gap-4">
      <{{pascalCase name}} variant="primary">Primary</{{pascalCase name}}>
      <{{pascalCase name}} variant="secondary">Secondary</{{pascalCase name}}>
      <{{pascalCase name}} variant="ghost">Ghost</{{pascalCase name}}>
      <{{pascalCase name}} variant="outline">Outline</{{pascalCase name}}>
      <{{pascalCase name}} variant="success">Success</{{pascalCase name}}>
      <{{pascalCase name}} variant="warning">Warning</{{pascalCase name}}>
      <{{pascalCase name}} variant="danger">Danger</{{pascalCase name}}>
      <{{pascalCase name}} variant="info">Info</{{pascalCase name}}>
    </div>
  );
};`,
      },
    },
  },
  render: (args): React.JSX.Element => (
    <div className="flex flex-wrap items-end gap-4">
      <{{pascalCase name}} {...args} variant="primary">Primary</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="secondary">Secondary</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="ghost">Ghost</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="outline">Outline</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="success">Success</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="warning">Warning</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="danger">Danger</{{pascalCase name}}>
      <{{pascalCase name}} {...args} variant="info">Info</{{pascalCase name}}>
    </div>
  ),
};

const Sizes: Story = {
  parameters: {
    docs: {
      source: {
        code: `import { {{pascalCase name}} } from "@fried-ui/react";

const Sizes = () => {
  return (
    <div className="flex items-end gap-4">
      <{{pascalCase name}} size="sm">Small {{pascalCase name}}</{{pascalCase name}}>
      <{{pascalCase name}} size="md">Medium {{pascalCase name}}</{{pascalCase name}}>
      <{{pascalCase name}} size="lg">Large {{pascalCase name}}</{{pascalCase name}}>
    </div>
  );
};`,
      },
    },
  },
  render: (args): React.JSX.Element => (
    <div className="flex items-end gap-4">
      <{{pascalCase name}} {...args} size="sm">Small {{pascalCase name}}</{{pascalCase name}}>
      <{{pascalCase name}} {...args} size="md">Medium {{pascalCase name}}</{{pascalCase name}}>
      <{{pascalCase name}} {...args} size="lg">Large {{pascalCase name}}</{{pascalCase name}}>
    </div>
  ),
};

const Disabled: Story = {
  parameters: {
    docs: {
      source: {
        code: `import { {{pascalCase name}} } from "@fried-ui/react";

const Disabled = () => {
  return <{{pascalCase name}} isDisabled>Disabled</{{pascalCase name}}>;
};`,
      },
    },
  },
  args: {
    children: "Disabled",
    isDisabled: true,
  },
};

export { Default, Variants, Sizes, Disabled };

export default meta;
