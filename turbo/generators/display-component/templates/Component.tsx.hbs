"use client";

import type { ComponentPropsWithRef } from "react";

import { clsx } from "clsx";

import { bem } from "../../utils/bem";

import type { {{pascalCase name}}VariantsProps } from "./{{dashCase name}}.variants";

export type {{pascalCase name}}Props = Omit<ComponentPropsWithRef<"{{element}}">, "className"> &
  {{pascalCase name}}VariantsProps & {
    className?: string;
  };

const {{pascalCase name}} = (props: Readonly<{{pascalCase name}}Props>) => {
  const { children, className, ref, size, variant, ...rest } = props;

  const {{camelCase name}}ClassName = clsx(
    bem({
      block: "fri-{{dashCase name}}",
      modifiers: {
        variant,
        size,
      },
    }),
    className,
  );

  return (
    <{{element}} data-slot="{{dashCase name}}" className={ {{camelCase name}}ClassName} ref={ref} {...rest}>
      {children}
    </{{element}}>
  );
};

{{pascalCase name}}.displayName = "{{pascalCase name}}";

export { {{pascalCase name}} };
