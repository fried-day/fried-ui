"use client";

/*
 * SCAFFOLD — single display component. If your component needs subparts (e.g., Card + CardHeader + CardBody),
 * use **named exports with the parent's prefix** (not dot notation). Example:
 *
 *   export { Card, CardHeader, CardBody, CardFooter };  // OK — shadcn pattern
 *   Card.Header = CardHeader;                            // BAD — dot notation banned
 *
 * See .claude/rules/architecture.md "Component Composition — Named Imports with Prefix".
 */

import type { ComponentPropsWithRef } from "react";

import { classes } from "../../utils/classes";

import type { {{pascalCase name}}VariantsProps } from "./{{dashCase name}}.variants";

export type {{pascalCase name}}Props = Omit<ComponentPropsWithRef<"div">, "className"> &
  {{pascalCase name}}VariantsProps & {
    className?: string;
  };

/**
 * TODO: Replace with one-line description of what {{pascalCase name}} does and when to use it.
 */
const {{pascalCase name}} = (props: Readonly<{{pascalCase name}}Props>) => {
  const { children, className, ref, size, variant, ...rest } = props;

  const componentClassName = classes({
    block: "{{dashCase name}}",
    modifiers: {
      variant,
      size,
    },
    className,
  });

  return (
    <div data-slot="{{dashCase name}}" className={componentClassName} ref={ref} {...rest}>
      {children}
    </div>
  );
};

{{pascalCase name}}.displayName = "{{pascalCase name}}";

export { {{pascalCase name}} };
